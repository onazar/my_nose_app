#hooks:
#  pre_setup: "cp /tmp/abc /tmp/ABC"

:ruby_version: 'ruby-2.1.2'

:python:
  python_version: 2.7
  pip_requirements_file: requirements.txt

:tests:
  - type: nosetests
    mode: basic
    output: exit-status
    files:
      - "test/number_test.py"
